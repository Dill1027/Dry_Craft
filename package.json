{
  "name": "dry-craft",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "vite": "^2.0.0"
  }
}