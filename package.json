{
  "name": "dry-craft",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "cd frontend && npm start",
    "build": "cd frontend && npm install && npm run build",
    "preview": "cd frontend && npm run build && npx serve build",
    "predeploy": "npm run build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "vite": "^2.0.0",
    "mongodb": "^6.3.0",
    "jsonwebtoken": "^9.0.2"
  }
}